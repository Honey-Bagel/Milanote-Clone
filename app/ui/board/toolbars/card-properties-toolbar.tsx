'use client';

import { TooltipProvider } from '@radix-ui/react-tooltip';
import { useCallback } from 'react';

export default function CardPropertiesToolbar() {
	<TooltipProvider>
		<div className="bg-[#0f172a]/95 backdrop-blur-sm border-b border-white/10 px-6 py-2 flex items-center shrink-0 z-40">
			
		</div>
	</TooltipProvider>
}